<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Product-searchextension</title>
    <style>
      html {
        box-sizing: border-box;
      }
      *,
      *:before,
      *:after {
        box-sizing: inherit;
      }
      html,
      body {
        height: 100%;
      }
      body {
        background: white;
        font-family: "Roboto", sans-serif;
      }
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }
      .web-similar-pages {
        display: flex;
        flex-direction: column;
        align-items: center;
      }

      h1 {
        margin-top: 1rem;
      }

      .card {
        width: 800px;
        height: 8rem;
        border: 1px solid #ddd;
        border-radius: 8px;
        overflow: hidden;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        display: flex;
        padding: 1rem;
      }

      .card-info {
        display: flex;
        flex-direction: column;
        flex-grow: 1;
        justify-content: start;
      }

      .product-website {
        background-color: rgb(199, 108, 75);
        color: white;
        margin-right: auto;
        padding: .1rem .5rem;
        border-radius: 8px;
        margin-left: .3rem;
      }

      a {
        text-decoration: none;
        margin: 0.5rem;
        display: inline-block;
        text-decoration: none;
        color: inherit;
      }

      .card img {
        width: 6rem;
        height: 6rem;
        display: block;
        object-fit: cover;
        margin-left: auto;
        border-radius: 5px;
      }

      .card h2 {
        font-size: 1.2rem;

        padding: 5px;
        margin: 10px 0;
        color: #333;
        text-decoration: none;
      }

      .card:hover {
        transform: translateY(-5px);
        transition: transform 0.3s ease;
      }
    </style>
  </head>
  <body>
    <div class="web-similar-pages">
      <h1>Similar Products</h1>
      <% let cnt = 0; %> <% if (pages && pages.length > 0) { %> <%
      pages.forEach(function(page) {%>

      <a href="<%= page.url %>"
        ><div class="card">
          <div class="card-info">
            <h2><%= page.pageTitle %></h2>
            <span class="product-website"><%= getMainDomain(page.url) %></span>
          </div>
          <img
            src="<%=page.fullMatchingImages.length>0?page.fullMatchingImages[0].url:page.partialMatchingImages[0].url%>"
            alt="<%= page.title %>"
          /></div
      ></a>

      <% }); %> <% } else { %>
      <p>No entries found.</p>
      <% } %>
    </div>
    <script>
      console.log(typeof "<%= pages.fullMatchingImages %>");
    </script>
  </body>
</html>
